{
  "name": "s02",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "start": "uvicorn app.main:app --reload",
    "test": "npx playwright test --project=chromium"
  },
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}